[pytest]
log_cli = true
log_cli_level = INFO

markers =
    crud: CRUD operations (create, read, update, delete resources)
    lifecycle: Resource lifecycle tests (upload, download, copy, move, trash)
    negative: Negative test scenarios (error handling, invalid input)
